import React from "react";
import { Layout } from "antd";

const { Header } = Layout;

const HomeHeader = () => (
  <Header style={{ background: "#fff", padding: 0, boxShadow:'0 0 5px #ccc' }} />
);

export default HomeHeader;
